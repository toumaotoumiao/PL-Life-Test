# CI-only synthetic integration fixtures

No personal archives. Only fixed fictional data. This directory is committed under `.github` and is not a part of the published 26 site files. The workflow runs on an ephemeral 127.0.0.1 random port. To keep report metadata redacted, do not add raw backups or screenshot uploads.
