# PL-Life v8.1.12.218 · Round141 Chromium CI 修复及后续诊断不中断

修复旧版 Round141 测试未注入多规则 CoC7 判定函数；Round141/148/149/151 失败时继续记录各自日志，末尾仍标红，供排查真实数据库恢复测试。程序资料、存储结构及导出逻辑未改变。

# PL-Life v8.1.12.217 · 恢复后刷新重开及二次 ZIP 验收

新增隔离浏览器原生 IndexedDB/localStorage 事务、ZIP 验证、刷新重开、二次备份和故障回滚 CI 验收。不会读取用户备份；环境拦截本地网页导航时仍须在 GitHub Actions/测试站补齐真实浏览器验收。档案 schema26、原始 Excel 和现有规则字段均未改写。

# PL-Life v8.1.12.216 · 多规则恢复后独立源证据校验

恢复前锁定 PC/模组/桌次的多规则关键数据；事务提交后从保存档案重新读取并对照原 ZIP 的预期值，不再只比较已变动的内存。恢复与发布验收仍需隔离环境及真实设备验证。

# PL-Life v8.1.12.215 · 多规则第六轮（历史档案恢复与自动清理安全）

- 修复重复 PC 自动清理误判规则专属模板及 Excel 差异为空。
- 多规则/旧版备份恢复核对；不改 schema26 和完整 ZIP 格式。
- 正式发布前须以真实备份在测试站只读核对并恢复验收。

# PL-Life v8.1.12.214 · 多规则第五轮（PC 规则专属轻量模板）

BRP 通用、Insane、忍神 PC 轻量资料模板分开保存，保护 CoC7 数值与 Excel，公开导出仅使用当前规则。

# PL-Life v8.1.12.213 · 多规则第四轮（规则筛选与统计）

模组和 PC 继承 v210–v212 的规则分类、原始数据保护与规则快照。本轮将同一“大类 → 具体规则 → 版次”筛选扩展至计划、记录、个人统计；旧桌次规则缺失时显式提供“规则未记录”，不按现今模组规则追溯改写。个人统计年度事件及图片导出与所选规则范围保持一致；全局搜索使用同一词库识别规则名称、官方别名及简称。个人偏好不增加“玩过／想玩／愿带／熟悉”字段，原有想跑／可带模组保持原样。修复旧版 CI 的写死版本号，自动化测试同步维护。schema26、Excel 原件、历史桌次及 ZIP 恢复格式不变。

# PL-Life v8.1.12.213 · 多规则第三轮（计划／记录规则快照）

本轮在 v211 的 PC／模组规则选择与筛选基础上，为新建开团计划和跑团记录加入独立的本桌规则快照：新桌继承所关联模组的规则；用户修改本桌规则后不因更换模组覆盖；归档和退回计划沿用原桌快照；旧记录未标明规则时保留“未记录”，而不是追随后来修改的模组规则。计划与记录编辑器共用既有固定分类词库；旧桌可以主动确认 CoC7。修复 v211 标准档案恢复核对投影漏带模组 `ruleMeta` 的问题，并补足弹层不裁切与手机操作热区。CoC Excel、PC 原始数据、schema26、现有 ZIP 格式均不变。Round146 已纳入 GitHub Actions。

# PL-Life v8.1.12.211 · 多规则第二轮（规则筛选与 PC 接入）

本轮延续 v210 固定的规则分类：PC 与模组使用同一套“大类 → 具体规则 → 版次”筛选，PC 编辑器增加同源规则选择器。CoC7 继续使用原有数值、技能和 Excel；其他规则先采用通用属性、技能、资源及完整人物资料，不凭空生成未经核实的专有字段。历史 CoC7 数值、成长快照和 Excel 原件在切换规则时保留；非 CoC7 的经历页不显示 CoC 专用 HP/SAN 成长录入。修复规则目录初始化顺序、招募与导出规则来源冲突；更新查询适配、移动端筛选、版本与 CI 回归。计划／记录规则快照已在 v212 接入，个人偏好规则选择仍待后续阶段。本轮不改 schema26 或 ZIP 恢复结构。

# v8.1.12.207 · Chromium 导出预览触控与 CI 诊断修复

修复 Round134 390px 导出底栏按钮被后置 CSS 压成 42px；320px 错误展开时预览正文恢复可见高度。更新合成浏览器测例，使弹窗结构与实际页面一致，失败时输出尺寸数据和截图，Actions 提前创建诊断附件目录并继续执行后续验收，再在结尾强制报告失败。原有 v205 PC 导出高密度双栏保持不变。

高密度和标准显示并排数值及技能，按实测文本高度分配。宽松仍为独立整行。自动分页与单张长图一致。

## v8.1.12.204 · 个人统计分区身份与显示范围

逐桌时间带、人物关系统计、模组统计和跑团足迹各自支持身份及部分／全部显示。年历与时间分布保持原状。分区折叠、导出内容和正式档案格式不变。

## v8.1.12.203 · 个人统计分区折叠

回顾年历、逐桌时间带、关系统计、模组统计、足迹与时间分布支持独立折叠；默认展开，同一标签页内保留状态。控制按钮与标题折叠按钮分开，导出内容、正式数据和备份格式不变。

## v8.1.12.202 · 正式页面流程验收第二轮

修复个人统计导出设置的运行期缺失数据函数：实际点击导出、切换预设并进入最终预览；无数据时区分“已选择但暂无数据”和“未选择区域”。手机计划编辑器分区跳转按钮恢复 40px 操作热区，底部保存栏收回可视范围。正式档案与备份格式不变。详见 pl200_audit.md。

## v8.1.12.199 · 正式页面流程验收第一轮

修复手机模组／跑团记录搜索区的遗留空网格，保留原有字段、按钮和正式数据。

## v8.1.12.198 · 全站画面精致化第五轮收口

仅修订图片导出及公共预览视觉，未改任何正式档案、数据读写、schema26、备份格式或导出像素。

## v8.1.12.197 · 个人偏好、个人统计和设置页画面精致化第四轮

仅调整现有 CSS 可读性和触控排布，不改变正式数据、schema26、备份格式和任何操作入口。

# PL-Life

PL收集梦想生活 · 正式站

https://toumaotoumiao.github.io/PL-Life/

## 正式站与测试站数据隔离

`/PL-Life/` 与 `/PL-Life-Test/` 同属 `toumaotoumiao.github.io` 这一浏览器 origin，不能仅靠路径区分 localStorage、IndexedDB、Cache Storage 或 BroadcastChannel。

从 v8.1.12.161 起，测试站使用独立运行命名空间，正式站沿用正式命名空间。正式站升级必须完整上传所有运行文件（包括 `sw.js`、查询桥接脚本和 `manifest.webmanifest`），不要把测试站文件与正式站文件混合发布。

浏览器本地档案不会因为覆盖 GitHub 程序文件而自动上传到服务器。重要档案仍应定期制作完整 ZIP 备份。

## 图片导出形式与实时预览（v8.1.12.194）

在个人偏好、个人统计、全年排期、PL/KP 跑团整理、单桌回顾、整合回顾、PC/模组档案等导出设置面板，均可选“自动分页”或“单张长图”。此选项和信息密度、隐私开关彼此独立，最终预览中仍可切换。详细时段等没有独立导出设置面板的功能，直接在统一最终预览里选择。

单张长图从原始内容区块连续绘制（PC/模组、统计、年度排期和整合回顾），不重复分页封面和页脚；源设置面板右侧会立即显示当前导出形式对应的版面，而不是仍显示旧分页。单张内容的编排器保留拖动手柄；多页模式可滚动查看全部页面。图片过长超出浏览器画布限制时明确提示并禁用本次错误导出，用户可以切回自动分页。偏好只保存在当前站点对应的浏览器本地存储，不涉及正式档案和备份数据。

发布时，完整仓库包请解压后将 `index.html`、`sw.js`、`.github` 等**包内根目录内容**上传至仓库根目录；仅运行文件包不包含 CI 配置。不要把压缩包当成一个子目录上传。

## 模组卡片补充资料（v8.1.12.190）

模组卡片直接展示所有已填写的地点、时代、人数、HO、时长、性质与再带意愿标签，无需点“更多资料”；空字段不占位置。简表只显示三项摘要，长备注仍可单独展开。此次变更仅调整列表展示，不修改正式档案或备份。

## v8.1.12.196 计划与记录视觉精修
- 日历取消按钮独立热区、记录正文与长 Log 完整换行；不变更数据结构与现有业务入口。

## v8.1.12.195 主列表视觉收口

PL/PC/模组卡片调整字号、行高、长标题与移动端触控热区。保留卡片原有全部数据与入口，不新增功能或迁移数据。
